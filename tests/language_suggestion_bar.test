<?php

/**
 * @file
 * Contains tests for the Language Suggestion Bar module.
 */

/**
 * Test case.
 */
class LanguageSuggestionBarTestCase extends DrupalWebTestCase {

  /**
   * Implements getInfo().
   */
  public static function getInfo() {
    return array(
      'name' => t('Language Suggestion Bar tests'),
      'description' => t('TODO: write me.'),
      'group' => t('Language Suggestion Bar'),
    );
  }

  /**
   * Implements setUp().
   */
  function setUp() {
    // Call the parent with an array of modules to enable for the test.
    parent::setUp(array('language_suggestion_bar'));

    // TODO: perform additional setup tasks here if required.
  }

  /**
   * Test the module's functionality.
   */
  function testTodoChangeThisName() {
    // TODO: write test!
  }

}
